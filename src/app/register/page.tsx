import RegisterUser from '@/components/auth/RegisterUser';

const Register = () => (
  <main className="flex flex-col justify-center min-h-screen p-12">
    <RegisterUser />
  </main>
);

export default Register;
